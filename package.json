{
  "name": "untitled",
  "version": "1.0.0",
  "description": "Habit Pilot workspace launcher",
  "main": "index.js",
  "scripts": {
    "dev": "npm --prefix ./pilot run dev",
    "build": "npm --prefix ./pilot run build",
    "preview": "npm --prefix ./pilot run preview",
    "lint": "npm --prefix ./pilot run lint"
  },
  "private": true
}
